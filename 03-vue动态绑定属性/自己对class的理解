总结:
①javascript中class代表一类样式的属性,类中包含相同的属性的模板'
②class关键字用于css中style标签中,将一类标签相同需要被修饰的内容抽取到一类的模板中.'
③即使不用class这个类模板，也可以直接定义style 标签属性对被修饰的标签内容进行修饰'
④定义,在< style></style>的标签内容中进行定义一类属性，以"."进行开头,例如：.a{},a为class对象'
⑤使用，在被修饰的标签上打上class关键字，并写入类名。例如:< h1 class="a">hello</h2>'
